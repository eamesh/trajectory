import { View } from '@tarojs/components';
import { defineComponent } from 'vue';

export default defineComponent({
  name: 'Map',

  setup () {

  },

  render () {
    return (
      <View class='page map'>
        map
      </View>
    );
  }
});
