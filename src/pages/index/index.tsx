import { defineComponent } from 'vue';
import { View } from '@tarojs/components';

export default defineComponent({
  name: 'Index',

  render () {
    return (
      <View class='page'>
        index
      </View>
    );
  }
});
