import { defineComponent } from 'vue';
import { View } from '@tarojs/components';

export default defineComponent({
  name: 'Me',

  render () {
    return (
      <View class='page'>
        me
      </View>
    );
  }
});
